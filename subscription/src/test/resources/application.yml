spring:
  profiles:
    active: test

  datasource:
    url: jdbc:h2:mem:iban
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
